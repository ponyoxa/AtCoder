#include <bits/stdc++.h>
using namespace std;

int main() {
    int N, D, P;
    cin >> N >> D >> P;
    
    vector<long long> F(N);
    for (int i = 0; i < N; ++i) {
        cin >> F[i];
    }
    
    // チケットの何セット買うのかの考え方がよくわからないな。
}
